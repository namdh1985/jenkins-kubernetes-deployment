apiVersion: v1
kind: Service
metadata:  
  name: service 
  namespace: jenkins
spec:
  selector:
    app: react-app 
  type: LoadBalancer 
  ports:
  - protocol: TCP
    port: 3000 
    targetPort: 3000 